<Window x:Class="CarDealer.WPF.Views.CarDealerMain"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.mcrosoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        Title="CarDealerMain" Height="500" Width="1025">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Год" VerticalAlignment="Center" Margin="0,0,3,0"/>
                <ComboBox ItemsSource="{Binding Years}" SelectedItem="{Binding SelectedYear}" Width="100" Margin="3"/>
                <Button Content="Рассчитать" ToolTip="Рассчитать стоимость за год" MinWidth="100" Margin="3" Command="{Binding CalcAllSalesCmd}"/>
                <Button Content="Экспорт в Excel" ToolTip="Рассчитать стоимость за год" MinWidth="100" Margin="3" Command="{Binding ExcelExportCmd}"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="5" Grid.Column="1" HorizontalAlignment="Right">
            <Button Content="Все авто" ToolTip="Выгрузить все доступные автомобили" MinWidth="100" Margin="3" Command="{Binding LoadAllCarsCmd}"/>
            <Button Content="Все продажи" ToolTip="Выгрузить все выполненые продажи" MinWidth="100" Margin="3" Command="{Binding LoadAllSalesCmd}"/>
        </StackPanel>


        <DataGrid ItemsSource="{Binding CarSales}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  Grid.Row="1"
                  Grid.ColumnSpan="2"
                  Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Модель" Binding="{Binding Model}" Width="150"/>
                <DataGridTextColumn Header="Январь" Binding="{Binding January}" Width="70"/>
                <DataGridTextColumn Header="Февраль" Binding="{Binding February}" Width="70"/>
                <DataGridTextColumn Header="Март" Binding="{Binding March}" Width="70"/>
                <DataGridTextColumn Header="Апрель" Binding="{Binding April}" Width="70"/>
                <DataGridTextColumn Header="Май" Binding="{Binding May}" Width="70"/>
                <DataGridTextColumn Header="Июнь" Binding="{Binding June}" Width="70"/>
                <DataGridTextColumn Header="Июль" Binding="{Binding July}" Width="70"/>
                <DataGridTextColumn Header="Август" Binding="{Binding August}" Width="70"/>
                <DataGridTextColumn Header="Сентябрь" Binding="{Binding September}" Width="70"/>
                <DataGridTextColumn Header="Октябрь" Binding="{Binding October}" Width="70"/>
                <DataGridTextColumn Header="Ноябрь" Binding="{Binding November}" Width="70"/>
                <DataGridTextColumn Header="Декабрь" Binding="{Binding December}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
