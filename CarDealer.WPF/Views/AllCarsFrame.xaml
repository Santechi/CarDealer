<Window x:Class="CarDealer.WPF.Views.AllCarsFrame"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.mcrosoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="AllCarsFrame" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Cars}"
                  AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  Grid.Row="1"
                  Margin="5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Бренд" Binding="{Binding Complect.Model.Brand.Name}" Width="140"/>
                <DataGridTextColumn Header="Модель" Binding="{Binding Complect.Model.Name}" Width="100"/>
                <DataGridTextColumn Header="Год выпуска" Binding="{Binding Year}" Width="80"/>
                <DataGridTextColumn Header="Страна производитель" Binding="{Binding Complect.Model.Brand.Country.Name}" Width="140"/>
                <DataGridTextColumn Header="Цвет" Binding="{Binding Color.Name}" Width="100"/>
                <DataGridTextColumn Header="Комплектация" Binding="{Binding Complect.Name}" Width="100"/>
                <DataGridTextColumn Header="Двигатель" Binding="{Binding Complect.Engine}" Width="100"/>
                <DataGridTextColumn Header="Коробка передач" Binding="{Binding Complect.Transmission}" Width="120"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
